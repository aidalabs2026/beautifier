---
import { SITE_NAME, FOOTER_LINKS } from '~/consts';
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-inner">
    <div>
      <strong>{SITE_NAME}</strong> &copy; {year}
      <div class="disclaimer">
        This tool is provided for informational purposes. Always validate output
        against your runtime's official parser for production use.
      </div>
    </div>
    <nav class="footer-links" aria-label="Legal">
      {FOOTER_LINKS.map((link) => (
        <a href={link.href}>{link.label}</a>
      ))}
    </nav>
  </div>
</footer>
